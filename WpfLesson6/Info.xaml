<Window x:Class="WpfLesson6.Info"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLesson6"
        mc:Ignorable="d"
        Title="Info" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label>Vendor</Label>
            <TextBlock Text="{Binding InfoCar.Vendor,UpdateSourceTrigger=PropertyChanged}" FontSize="26" ></TextBlock>
            <Label>Model</Label>
            <TextBlock Text="{Binding InfoCar.Model,UpdateSourceTrigger=PropertyChanged}" FontSize="26" ></TextBlock>
            <Label>Year</Label>
            <TextBlock Text="{Binding InfoCar.Year,UpdateSourceTrigger=PropertyChanged}" FontSize="26" ></TextBlock>
        </StackPanel>
    </Grid>
</Window>
